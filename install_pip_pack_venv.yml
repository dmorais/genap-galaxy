---
- name: Install PIP packages
  hosts: galaxyservers
  become_method: sudo
  become: yes
  remote_user: "{{ remote_user }}"
  tasks:
    - name: install uwsig
      become: yes
      become_user: "{{ galaxy_user }}"
      remote_user: "{{ remote_user }}"
      shell: source "{{ galaxy_venv_dir }}"/bin/activate && pip install uwsgi
    - name: install cython
      become: yes
      become_user: "{{ galaxy_user }}"
      remote_user: "{{ remote_user }}"
      shell: source "{{ galaxy_venv_dir }}"/bin/activate && pip install cython

#    - name: Move tools dir to server dir
#      command: mv "{{ galaxy_server_dir }}/tools" "{{ cvmfs_server_dir }}"
